# wiki/

The agent's living knowledge base. Read `SCHEMA.md` before any write.

- `index.md` — catalog of every page
- `log.md` — append-only operations log
- `PLAN.md` — master project plan
- `STATUS.md` — current state + blocker map
- `IDEAS.md` — numbered backlog
- `raw/` — immutable source files
- `sources/`, `entities/`, `concepts/`, `questions/` — agent-curated cards
- `plan/` — ADRs, packages, sub-plans
- `spikes/` — pre-Week-1 experiment findings

For humans: start at `index.md`. For agents: start at `SCHEMA.md`.
